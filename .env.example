# Required: OpenAI API Key for GPT-4 coaching responses
OPENAI_API_KEY=sk-proj-your-key-here

# Optional: Strict mode enforcement (default: 1)
REQUIRE_OPENAI_KEY=1

# Server configuration (Railway sets PORT automatically)
PORT=8000
HOST=0.0.0.0
